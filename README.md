# Neel's Portfolio Tracker

A Python Dash app to track stock portfolios. Enter tickers, shares, and purchase dates, then view your portfolio's value over time, table, and gain/loss stats.

## Requirements

- Python 3.10+
- run using "python app.py"
- Install dependencies:

```bash
pip install -r requirements.txt
